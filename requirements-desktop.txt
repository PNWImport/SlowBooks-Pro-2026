# Extra dependency for the native desktop install (desktop_launcher.py).
# Kept separate from requirements.txt so the Docker/server image and CI
# are unaffected. Version 6.2.1 includes the current macOS ARM64/window-close
# fixes and is pinned so desktop artifacts do not drift between builds.
pywebview==6.2.1
