# Dev / test dependencies. NOT installed into the production Docker image.
# Pull in with:  pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.3.0
pytest-cov>=5.0.0
# Pinned to the minor series the tree was swept against — a floating
# range silently changes what the lint gate enforces. See ci.yml.
black>=24.8,<25
ruff>=0.6,<0.7
